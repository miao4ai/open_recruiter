# LLM Provider — set one of the following
# Anthropic (recommended)
ANTHROPIC_API_KEY=sk-ant-...

# OpenAI (alternative)
OPENAI_API_KEY=sk-...

# Which provider to use: "anthropic" or "openai"
LLM_PROVIDER=anthropic

# Model name (optional, defaults are sensible)
# LLM_MODEL=claude-sonnet-4-20250514

# Email — set one of the following
# SendGrid
# SENDGRID_API_KEY=SG....
# EMAIL_FROM=recruiter@yourcompany.com

# Gmail (OAuth)
# GMAIL_CREDENTIALS_FILE=credentials.json

# Console mode (default for MVP — prints emails to terminal)
EMAIL_BACKEND=console
