[project]
name = "recruitgpt"
version = "0.0.0"
description = "RecruitGPT — training and inference for recruiting-domain language models."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["recruitgpt"]
