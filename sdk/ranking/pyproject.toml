[project]
name = "openrecruiter-ranking"
version = "0.0.0"
description = "Unbiased candidate ranking for Open Recruiter."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["openrecruiter_ranking"]
