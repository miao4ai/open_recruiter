# openrecruiter-ranking

Unbiased candidate ranking. Specification pending.

**Status: scaffold.** No implementation yet — see the repo root [README](../../README.md) for where this sits in the plan.

## Install

```bash
pip install openrecruiter-ranking
```

## Build from source

```bash
cd sdk/ranking
uv build --out-dir dist   # -> dist/*.whl, dist/*.tar.gz
```
