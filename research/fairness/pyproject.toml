[project]
name = "openrecruiter-research-fairness"
version = "0.0.0"
description = "Reproducible experiments behind openrecruiter-fairness."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["openrecruiter_research_fairness"]
