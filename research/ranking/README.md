# openrecruiter-research-ranking

Reproducible experiments, benchmarks, and ablations behind `openrecruiter-ranking`. Specification pending.

**Status: scaffold.** No implementation yet — see the repo root [README](../../README.md) for where this sits in the plan.

## Install

```bash
pip install openrecruiter-research-ranking
```

## Build from source

```bash
cd research/ranking
uv build --out-dir dist   # -> dist/*.whl, dist/*.tar.gz
```
