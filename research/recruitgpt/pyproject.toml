[project]
name = "recruitgpt-research"
version = "0.0.0"
description = "Reproducible experiments behind recruitgpt."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["recruitgpt_research"]
